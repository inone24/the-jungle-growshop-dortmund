---
import meta from '../../content/seo/growshop.json';
import site from '../../content/global.json';
import PageLayout from '../layouts/PageLayout.astro';

const canonical = meta.canonical.trim();
const breadcrumb = [
  { name: 'Start', url: 'https://the-jungle-growshop-dortmund.github.io/' },
  { name: 'Growshop Dortmund', url: canonical }
];

const heroHighlights = [
  {
    icon: '🛠️',
    title: 'Smart-Grow vor Ort',
    description: 'UIS, GrowHub & App-Steuerung live testen.'
  },
  { icon: '⚡', title: 'Sofort verfügbar', description: 'Zelte, LEDs, AKF/EC, Dünger & Zubehör im Lager.' },
  {
    icon: '📐',
    title: 'Setup-Berechnung',
    description: 'Watt, Luftwechsel & VPD gemeinsam dimensionieren.'
  },
  {
    icon: '🔄',
    title: 'Service & Ersatz',
    description: 'Troubleshooting, Ersatzteile und Nachrüstungen.'
  }
];

const heroShortcuts = [
  { label: 'Smart Systems', href: '#smart_systems_spotlight' },
  { label: 'Setup Ablauf', href: '#setup_timeline' },
  { label: 'Services', href: '#service_cards' },
  { label: 'Kontakt', href: '#reach_us' }
];

const smartSystems = [
  {
    title: 'AC Infinity · UIS Ecosystem',
    description:
      'Controller 69 Pro/Pro+, Sunrise/Sunset, VPD-Automation. Kombinierbar mit EC-Lüftern, LEDs & Sensoren.',
    features: [
      'UIS-Kits für Zelte von 60×60 bis 150×300 cm',
      'Automatisierte Klima-Workflows inkl. Nachtabsenkung',
      'Daisy-Chain-kompatible LEDs & Inline-Fans'
    ]
  },
  {
    title: 'VIVOSUN · GrowHub',
    description:
      'Automationen, Rezepte & Cloud-Diary. Daisy-Chain-LEDs, Klima & Bewässerung zentral orchestriert.',
    features: [
      'GrowHub E42/E52 Controller mit App-Monitoring',
      'Rezepte & Automationen für Licht, Klima & Bewässerung',
      'Sensorintegration für VPD, EC & Drain-Checks'
    ]
  }
];

const setupSteps = [
  'Ziel & Fläche definieren',
  'Dimensionierung von Licht & Luft',
  'Klima- & Steuerung auswählen',
  'App-Pairing & Sensorik',
  'Feintuning & Schalldämmung',
  'Daten nutzen & erweitern'
];

const services = [
  {
    icon: '📝',
    title: 'Quick-Check',
    description: 'Skizzen, Maße, Fotos – wir rechnen Watt & Luftwechsel live am Tresen.'
  },
  {
    icon: '🧰',
    title: 'Komplett-Set',
    description: 'Zelt, LED-Bar, EC-Lüfter/AKF, Controller & Sensoren – sauber kombiniert.'
  },
  {
    icon: '🧪',
    title: 'Troubleshooting',
    description: 'Geruch, Hitze, Feuchte, pH/EC – Ursachen finden & Lösungen mitgeben.'
  },
  {
    icon: '🔧',
    title: 'Ersatz & Zubehör',
    description: 'Filter, Schlauch, Schalldämpfer, Adapter, Aufhängungen & mehr sofort verfügbar.'
  }
];

const contactChips = [...site.payments, site.hours, ...site.transport];

const faq = [
  {
    question: 'Welche LED passt zu 100×100?',
    answer:
      'Meist 250–350 W High-End-Bar oder Kombination. Dimmer & Controller für Nachtabsenkung/Boost einplanen.'
  },
  {
    question: 'Welcher AKF & EC-Lüfter?',
    answer:
      'Volumen × Luftwechsel + Reserve für Filter & Schlauchführung. Wir rechnen das im Laden mit dir durch.'
  }
];
---

<PageLayout
  title={meta.title}
  description={meta.description}
  canonical={canonical}
  breadcrumb={breadcrumb}
  faq={faq}
>
  <section id="hero" class="mb-16" aria-labelledby="hero-heading">
    <div class="grid gap-10 lg:grid-cols-[minmax(0,1.1fr)_minmax(0,0.9fr)]">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-accent-primary">Growshop Dortmund</p>
        <h1 id="hero-heading" class="mt-4 font-display text-3xl text-text-high sm:text-4xl">
          Ehrliche Beratung, Smart-Grow zum Anfassen – sofort verfügbar
        </h1>
        <p class="mt-4 max-w-3xl text-text-dim">
          Wir kombinieren Licht, Klima und Steuerung zu Setups, die greifen. Bring Maße, Fotos &amp; Ziele
          mit – wir rechnen Watt, Luftwechsel, VPD und Schalldämmung live am Tresen.
        </p>
        <div class="mt-8 flex flex-wrap gap-3">
          <a
            class="
              rounded-full bg-accent-moss/10 px-5 py-2 text-sm font-semibold text-accent-primary
              shadow-[var(--glow-alt)] transition hover:bg-accent-moss/20 focus-ring
            "
            href="/kontakt/"
            aria-label="Beratung vereinbaren und Kontakt aufnehmen"
          >
            Beratung &amp; Kauf
          </a>
          <a
            class="
              rounded-full border border-accent-moss/40 px-5 py-2 text-sm font-semibold text-accent-primary
              transition hover:border-accent-moss/60 focus-ring
            "
            href="/standort/"
            aria-label="Route zum Jungle Growshop Dortmund planen"
          >
            Route starten
          </a>
          <a
            class="
              rounded-full border border-white/20 px-5 py-2 text-sm font-semibold text-text-high
              transition hover:border-white/40 focus-ring
            "
            href={site.phone_link}
            aria-label={`Jetzt im Jungle Growshop Dortmund anrufen unter ${site.phone_display}`}
          >
            {site.phone_display}
          </a>
        </div>
        <nav aria-label="Bereiche auf dieser Seite" class="mt-6">
          <ul class="flex flex-wrap gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-text-dim">
            {heroShortcuts.map((item) => (
              <li>
                <a
                  class="
                    glass-panel inline-flex items-center gap-2 rounded-full px-4 py-2 text-[0.7rem]
                    text-text-high transition hover:text-accent-primary focus-ring
                  "
                  href={item.href}
                >
                  <span aria-hidden="true">↘</span>
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </div>
      <aside class="glass-panel flex flex-col justify-between rounded-2xl p-6 shadow-card" aria-label="Highlights">
        <ul class="space-y-4">
          {heroHighlights.map((item) => (
            <li class="flex gap-3">
              <span class="flex size-10 shrink-0 items-center justify-center rounded-full bg-accent-moss/10 text-xl" aria-hidden="true">
                {item.icon}
              </span>
              <div>
                <h2 class="font-display text-lg text-accent-primary">{item.title}</h2>
                <p class="mt-1 text-sm text-text-dim">{item.description}</p>
              </div>
            </li>
          ))}
        </ul>
        <dl class="mt-6 grid grid-cols-2 gap-3 text-xs text-text-dim">
          <div class="glass-panel rounded-xl px-4 py-3">
            <dt class="font-semibold text-text-high">Adresse</dt>
            <dd class="mt-1">{site.address}</dd>
          </div>
          <div class="glass-panel rounded-xl px-4 py-3">
            <dt class="font-semibold text-text-high">Öffnungszeiten</dt>
            <dd class="mt-1">{site.hours}</dd>
          </div>
        </dl>
      </aside>
    </div>
  </section>

  <section
    id="smart_systems_spotlight"
    class="mt-20"
    aria-labelledby="smart-systems"
  >
    <div class="flex items-baseline justify-between gap-4">
      <div>
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">Smart Systems</p>
        <h2 id="smart-systems" class="mt-3 font-display text-2xl text-text-high">
          Spotlight: Automatisierung &amp; App-Steuerung
        </h2>
      </div>
    </div>
    <div class="mt-6 grid gap-6 md:grid-cols-2">
      {smartSystems.map((system) => (
        <article class="glass-panel flex h-full flex-col rounded-2xl p-6 shadow-card">
          <h3 class="font-display text-xl text-accent-primary">{system.title}</h3>
          <p class="mt-3 text-sm text-text-dim">{system.description}</p>
          <ul class="mt-4 space-y-2 text-sm text-text-dim">
            {system.features.map((feature) => (
              <li class="flex items-start gap-2">
                <span class="mt-[3px] text-accent-primary" aria-hidden="true">•</span>
                <span>{feature}</span>
              </li>
            ))}
          </ul>
        </article>
      ))}
    </div>
  </section>

  <section
    id="setup_timeline"
    class="mt-20"
    aria-labelledby="setup-timeline"
  >
    <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">Ablauf</p>
    <h2 id="setup-timeline" class="mt-3 font-display text-2xl text-text-high">In sechs Schritten zum Setup</h2>
    <ol class="mt-6 grid gap-4 md:grid-cols-3">
      {setupSteps.map((step, index) => (
        <li class="glass-panel rounded-2xl p-5 text-sm text-text-high shadow-card">
          <span class="font-display text-accent-primary">{index + 1}&nbsp;·&nbsp;</span>
          {step}
        </li>
      ))}
    </ol>
  </section>

  <section
    id="service_cards"
    class="mt-20"
    aria-labelledby="services"
  >
    <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">Services</p>
    <h2 id="services" class="mt-3 font-display text-2xl text-text-high">Warum The Jungle?</h2>
    <div class="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">
      {services.map((service) => (
        <article class="glass-panel flex h-full flex-col gap-3 rounded-2xl p-6 shadow-card">
          <div class="flex items-center gap-3">
            <span class="text-2xl" aria-hidden="true">{service.icon}</span>
            <h3 class="font-display text-lg text-accent-primary">{service.title}</h3>
          </div>
          <p class="text-sm text-text-dim">{service.description}</p>
        </article>
      ))}
    </div>
  </section>

  <section
    id="reach_us"
    class="mt-20 grid gap-8 lg:grid-cols-[1.1fr_0.9fr]"
    aria-labelledby="reach-us"
  >
    <div class="glass-panel rounded-2xl p-6 shadow-card">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">Standort</p>
      <h2 id="reach-us" class="mt-3 font-display text-2xl text-text-high">Unser Standort in Dortmund</h2>
      <address class="mt-4 not-italic text-sm text-text-dim">
        {site.address}<br />
        <a class="hover:text-accent-primary focus-ring" href={site.phone_link}>{site.phone_display}</a><br />
        <a class="hover:text-accent-primary focus-ring" href={`mailto:${site.email.trim()}`}>{site.email.trim()}</a><br />
        {site.hours}
      </address>
      <ul class="mt-4 flex flex-wrap gap-2 text-xs text-text-dim">
        {contactChips.map((chip) => (
          <li class="glass-panel inline-flex items-center rounded-full px-3 py-1">{chip}</li>
        ))}
      </ul>
      <img
        class="mt-6 w-full rounded-xl border border-white/10"
        src="/map-preview.svg"
        alt="Karte mit Markierung für The Jungle Growshop Dortmund"
        loading="lazy"
      />
      <div class="mt-6 flex flex-wrap gap-3">
        <a
          class="
            rounded-full bg-accent-moss/10 px-4 py-2 text-sm font-semibold text-accent-primary
            shadow-[var(--glow-alt)] transition hover:bg-accent-moss/20 focus-ring
          "
          href="/standort/"
          aria-label="Route zum Jungle Growshop Dortmund anzeigen"
        >
          Route planen
        </a>
        <a
          class="
            rounded-full border border-accent-moss/40 px-4 py-2 text-sm font-semibold text-accent-primary
            transition hover:border-accent-moss/60 focus-ring
          "
          href={site.phone_link}
          aria-label={`Jetzt telefonisch anfragen unter ${site.phone_display}`}
        >
          Jetzt anrufen
        </a>
      </div>
    </div>
    <aside class="glass-panel rounded-2xl p-6 shadow-card">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">Team</p>
      <h2 class="mt-3 font-display text-2xl text-text-high">Team Jungle</h2>
      <p class="mt-3 text-sm text-text-dim">
        Klartext statt Sales-Talk: Wir planen Setups, die greifen. Von Schalldämmung bis Sensorik – wir
        zeigen dir live, was funktioniert und wie du es erweiterst.
      </p>
      <div class="mt-6 flex flex-wrap gap-3">
        <a
          class="
            rounded-full border border-white/15 px-4 py-2 text-sm font-semibold text-text-high
            transition hover:border-white/40 focus-ring
          "
          href="/team/"
          aria-label="Mehr über das Team Jungle erfahren"
        >
          Team kennenlernen
        </a>
        <a
          class="
            rounded-full bg-white px-4 py-2 text-sm font-semibold text-background-dark transition
            hover:bg-white/90 focus-ring
          "
          href="/kontakt/"
          aria-label="Kontaktformular des Jungle Growshop öffnen"
        >
          Nachricht senden
        </a>
      </div>
    </aside>
  </section>

  <section
    id="faq"
    class="mt-20"
    aria-labelledby="faq-heading"
  >
    <p class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-primary">FAQ</p>
    <h2 id="faq-heading" class="mt-3 font-display text-2xl text-text-high">Häufig gestellte Fragen</h2>
    <div class="mt-6 space-y-4">
      {faq.map((item) => (
        <details class="glass-panel rounded-2xl p-5 text-left shadow-card">
          <summary class="cursor-pointer font-display text-lg text-accent-primary focus:outline-none focus-ring">
            {item.question}
          </summary>
          <p class="mt-3 text-sm text-text-dim">{item.answer}</p>
        </details>
      ))}
    </div>
  </section>

  <section id="cta" class="mt-20 text-center">
    <div class="glass-panel inline-flex flex-col items-center gap-4 rounded-2xl px-8 py-10 shadow-card">
        <h2 class="font-display text-2xl text-text-high">Einmal sauber planen – entspannt ernten</h2>
        <p class="max-w-2xl text-sm text-text-dim">
          Ob Smart-Grow, klassisches Zelt oder Troubleshooting: Wir nehmen uns Zeit für dein Projekt und
          haben die passenden Komponenten direkt im Lager.
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          <a
            class="
              rounded-full bg-accent-moss/10 px-6 py-3 text-sm font-semibold text-accent-primary
              shadow-[var(--glow-alt)] transition hover:bg-accent-moss/20 focus-ring
            "
            href="/kontakt/"
            aria-label="Beratung für Growsetup anfragen"
          >
            Beratung anfragen
          </a>
          <a
            class="
              rounded-full border border-accent-moss/40 px-6 py-3 text-sm font-semibold text-accent-primary
              transition hover:border-accent-moss/60 focus-ring
            "
            href={site.phone_link}
            aria-label={`Telefonische Beratung unter ${site.phone_display} vereinbaren`}
          >
            Direkt anrufen
          </a>
        </div>
    </div>
  </section>
</PageLayout>
