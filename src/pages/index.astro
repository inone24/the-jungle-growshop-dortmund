---
import BaseLayout from '../layouts/BaseLayout.astro';
import type { GlobalContent, StructuredData } from '../types';
import seo from '../../content/seo/home.json';
import global from '../../content/global.json';

const globalData = global as GlobalContent;

const localBusiness: StructuredData = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: globalData.businessName,
  legalName: globalData.legalName,
  url: globalData.siteUrl,
  telephone: globalData.contact.phone,
  email: globalData.contact.email,
  image: `${globalData.siteUrl}favicon.svg`,
  address: {
    '@type': 'PostalAddress',
    streetAddress: globalData.address.street,
    postalCode: globalData.address.postalCode,
    addressLocality: globalData.address.city,
    addressCountry: globalData.address.country,
  },
  openingHoursSpecification: globalData.openingHours.map((slot) => ({
    '@type': 'OpeningHoursSpecification',
    description: slot,
  })),
  geo: {
    '@type': 'GeoCoordinates',
    latitude: globalData.geo.latitude,
    longitude: globalData.geo.longitude,
  },
  paymentAccepted: globalData.paymentMethods.join(', '),
};

const breadcrumbs = [
  { name: 'Startseite', href: '/' },
];
---
<BaseLayout seo={seo} breadcrumbs={breadcrumbs} structuredData={localBusiness}>
  <section class="relative overflow-hidden">
    <div class="mx-auto grid max-w-6xl gap-12 px-4 py-20 sm:grid-cols-2 sm:px-6">
      <div class="space-y-6">
        <span class="inline-flex rounded-full bg-brand-light/10 px-4 py-1 text-xs uppercase tracking-[0.2em] text-brand-light">
          Urban Growing since 2012
        </span>
        <h1 class="text-4xl font-heading text-white sm:text-5xl">
          Dortmunds Urban-Gardening-Hub
        </h1>
        <p class="text-lg text-slate-300">
          Wir unterstützen dich bei jedem Schritt – von der Planung über die Auswahl des richtigen Equipments
          bis zur nachhaltigen Pflege deiner Pflanzen. Komm vorbei oder ruf uns an, wir freuen uns auf dein Projekt!
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/produkte/" class="rounded-full bg-brand-light px-6 py-3 font-semibold text-slate-900 shadow-lg shadow-brand-light/30 transition hover:bg-emerald-300">
            Sortiment entdecken
          </a>
          <a href="/kontakt/" class="rounded-full border border-white/20 px-6 py-3 font-semibold text-white transition hover:border-brand-light hover:text-brand-light">
            Beratung vereinbaren
          </a>
        </div>
      </div>
      <div class="relative">
        <div class="absolute inset-0 -left-10 rounded-full bg-brand-light/10 blur-3xl"></div>
        <div class="relative space-y-4 rounded-3xl border border-white/10 bg-white/5 p-6 backdrop-blur">
          <h2 class="text-xl font-heading text-brand-light">Highlights</h2>
          <ul class="space-y-3 text-sm text-slate-200">
            <li>🌱 Indoor- und Outdoor-Growlösungen</li>
            <li>💡 Energieeffiziente LED-Systeme</li>
            <li>🧪 Premium-Nährstoffe & Booster</li>
            <li>📦 Diskreter Versand & Click & Collect</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-slate-950/60">
    <div class="mx-auto max-w-6xl space-y-10 px-4 py-16 sm:px-6">
      <h2 class="text-3xl font-heading text-brand-light">Warum The Jungle?</h2>
      <div class="grid gap-8 sm:grid-cols-3">
        <article class="rounded-2xl border border-white/5 bg-white/5 p-6">
          <h3 class="text-xl font-semibold text-white">Kompetente Beratung</h3>
          <p class="mt-3 text-sm text-slate-300">
            Unser Team besteht aus erfahrenen Growern und Urban-Gardening-Fans, die ihr Wissen gerne teilen – ob Anfänger:in oder Profi.
          </p>
        </article>
        <article class="rounded-2xl border border-white/5 bg-white/5 p-6">
          <h3 class="text-xl font-semibold text-white">Kuratiertes Sortiment</h3>
          <p class="mt-3 text-sm text-slate-300">
            Wir führen Brands, denen wir vertrauen: langlebiges Equipment, nachhaltige Materialien und smarte Lösungen für jede Fläche.
          </p>
        </article>
        <article class="rounded-2xl border border-white/5 bg-white/5 p-6">
          <h3 class="text-xl font-semibold text-white">Community-Spirit</h3>
          <p class="mt-3 text-sm text-slate-300">
            Workshops, Events und regelmäßige Updates – wir vernetzen die Grow-Community im Ruhrgebiet und darüber hinaus.
          </p>
        </article>
      </div>
    </div>
  </section>

  <section class="border-y border-white/5 bg-slate-900/60">
    <div class="mx-auto grid max-w-6xl gap-10 px-4 py-16 sm:grid-cols-2 sm:px-6">
      <div class="space-y-4">
        <h2 class="text-3xl font-heading text-brand-light">Neu im Sortiment</h2>
        <p class="text-sm text-slate-300">
          Jede Woche frische Highlights aus unserem Shop – von smarten Bewässerungssystemen über klimafreundliche
          Beleuchtung bis hin zu praktischen Headshop-Accessoires.
        </p>
      </div>
      <div class="grid gap-4 sm:grid-cols-2">
        {['LED Full Spectrum', 'CO₂-optimierte Dünger', 'Bio-Substrate', 'Starter-Kits'].map((item) => (
          <div class="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-slate-200">
            {item}
          </div>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
