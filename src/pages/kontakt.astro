---
import BaseLayout from '../layouts/BaseLayout.astro';
import seo from '../../content/seo/kontakt.json';
import global from '../../content/global.json';
import type { GlobalContent } from '../types';

const globalData = global as GlobalContent;

const breadcrumbs = [
  { name: 'Startseite', href: '/' },
  { name: 'Kontakt', href: '/kontakt/' },
];
---
<BaseLayout seo={seo} breadcrumbs={breadcrumbs}>
  <section class="bg-slate-950/60">
    <div class="mx-auto grid max-w-6xl gap-10 px-4 py-16 sm:grid-cols-2 sm:px-6">
      <div class="space-y-6">
        <h1 class="text-4xl font-heading text-brand-light">Meld dich bei uns</h1>
        <p class="text-lg text-slate-300">
          Egal ob du Fragen zum Sortiment hast, einen Workshop buchen möchtest oder individuelle Beratung brauchst –
          wir sind per Telefon, Mail und vor Ort für dich da.
        </p>
        <div class="space-y-3 rounded-3xl border border-white/5 bg-white/5 p-6 text-sm text-slate-200">
          <p><strong>Telefon:</strong> <a href={`tel:${globalData.contact.phone.replace(/\s+/g, '')}`}>{globalData.contact.phone}</a></p>
          <p><strong>Email:</strong> <a href={`mailto:${globalData.contact.email}`}>{globalData.contact.email}</a></p>
          <p><strong>Adresse:</strong> {globalData.address.street}, {globalData.address.postalCode} {globalData.address.city}</p>
        </div>
        <div>
          <h2 class="text-xl font-heading text-brand-light">Social Media</h2>
          <p class="mt-2 text-sm text-slate-300">Folge uns für Produkt-News, Events und Grow-Tipps.</p>
          <div class="mt-3 flex gap-4 text-lg">
            <a href={globalData.socials.instagram} target="_blank" rel="noreferrer">Instagram</a>
            <a href={globalData.socials.facebook} target="_blank" rel="noreferrer">Facebook</a>
          </div>
        </div>
      </div>
      <div class="space-y-6 rounded-3xl border border-white/5 bg-white/5 p-6">
        <h2 class="text-2xl font-heading text-brand-light">Schreib uns direkt</h2>
        <form class="space-y-4">
          <div class="space-y-2">
            <label class="text-sm text-slate-300" for="name">Name</label>
            <input
              id="name"
              name="name"
              type="text"
              required
              placeholder="Dein Name"
              class="w-full rounded-full border border-white/10 bg-slate-950/80 px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:border-brand-light focus:outline-none"
            />
          </div>
          <div class="space-y-2">
            <label class="text-sm text-slate-300" for="email">E-Mail</label>
            <input
              id="email"
              name="email"
              type="email"
              required
              placeholder="deine@email.de"
              class="w-full rounded-full border border-white/10 bg-slate-950/80 px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:border-brand-light focus:outline-none"
            />
          </div>
          <div class="space-y-2">
            <label class="text-sm text-slate-300" for="message">Nachricht</label>
            <textarea
              id="message"
              name="message"
              rows="5"
              required
              placeholder="Erzähl uns von deinem Projekt"
              class="w-full rounded-3xl border border-white/10 bg-slate-950/80 px-4 py-3 text-sm text-white placeholder:text-slate-500 focus:border-brand-light focus:outline-none"
            ></textarea>
          </div>
          <button type="submit" class="w-full rounded-full bg-brand-light px-6 py-3 text-sm font-semibold text-slate-900 transition hover:bg-emerald-300">
            Nachricht senden
          </button>
          <p class="text-xs text-slate-400">
            Wir melden uns in der Regel innerhalb von 24 Stunden. Deine Daten behandeln wir vertraulich gemäß unserer
            <a href="/datenschutz/" class="ml-1 underline">Datenschutzerklärung</a>.
          </p>
        </form>
      </div>
    </div>
  </section>
</BaseLayout>
